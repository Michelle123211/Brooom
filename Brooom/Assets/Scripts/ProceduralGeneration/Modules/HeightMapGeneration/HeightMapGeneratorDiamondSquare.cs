using System.Collections;
using System.Collections.Generic;
using UnityEngine;


// TODO: Define significant points (top of the mountain, tops of hills and valleys between them), complete a lower resolution heightmap and then complete it using diamond-square algorithm
// TODO: May start from regions already determined or determine them along the way
public class HeightMapGeneratorDiamondSquare : MonoBehaviour {

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
